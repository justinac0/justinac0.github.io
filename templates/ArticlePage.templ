package templates

import "justinac0.github.io/internal/page"

templ ArticlePage(p page.Page) {
	@ArticleLayout(p.Meta.Title) {
		<hr/>
		<a href="/">back</a>
		<article>
			@templ.Raw(p.HTML)
		</article>
	}
}
